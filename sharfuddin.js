let myNumber = 5;
//myNumber= -2;

if (myNumber >= 0) {
    console.log(myNumber,"ist eine positive Zahlt");
//ausgeführt () wahr ist
} else {
    // else wird automatisch ausgefuhrt, wenn die if-Bedingung
    //*nicht* erfühllt ist
    console.log(myNumber, "ist eine negative Zahl");
}